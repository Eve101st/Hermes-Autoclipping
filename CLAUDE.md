# CLAUDE.md

**Read [`AGENTS.md`](./AGENTS.md) first — it is the single source of truth for this
project.** This stub exists only so Claude Code auto-loads that context. All
guidance (architecture, hosting/git, build, tools, services, manual setup, open
items) lives in `AGENTS.md`.

Text model is **Owl Alpha** via Hermes — no Anthropic key. Do not commit or push
unless explicitly asked.
