# Secrets — provided via the process environment on the VPS, never committed
.env

# Internal-only docs (host/account specifics + running status log) — kept local,
# never committed, so the repo is safe to make public.
INTERNAL.md
PROJECT_STATUS.md

# Claude Code local settings
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
